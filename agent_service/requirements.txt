# AI Agent Service — pin theo đúng version đang cài trên Jetson
# (khác backend/requirements.txt: ở đó langgraph/langchain đang bị comment out)

fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0
python-dotenv==1.0.0
python-multipart==0.0.6

# JWT + mật khẩu — service tự cấp token được (dùng chung SECRET_KEY với backend)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.0

# MongoDB — dùng chung DB với backend
motor>=3.3.0
pymongo>=4.5.0

# LangGraph / LangChain stack
langgraph==1.0.7
langchain==1.2.6
langchain-core==1.2.7
langchain-openai==1.1.7
openai==2.15.0
tiktoken==0.7.0

# Tools
psutil==5.9.8
httpx>=0.27.0
